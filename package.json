{
  "name": "app-noel",
  "version": "1.0.0",
  "scripts": {
    "heroku-prebuild": "cd front && npm install && npm run build && cd ../back && npm install",
    "heroku-postbuild": "npm run build --prefix front",
    "start": "node back/index.js"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}
