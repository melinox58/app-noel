{
  "name": "app-noel",
  "version": "1.0.0",
  "scripts": {
    "heroku-prebuild": "npm install --prefix back && npm install --prefix front && npm install -g @vue/cli",
    "heroku-postbuild": "npm run build --prefix front",
    "start": "node back/index.js"
  },
  "engines": {
    "node": "20.x",
    "npm": "11.0.0"
  }
}
