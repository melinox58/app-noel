{
  "name": "app-noel",
  "version": "1.0.0",
  "scripts": {
    "heroku-prebuild": "npm install --prefix back && npm install --prefix front && npx vue-cli-service build --prefix front",
    "heroku-postbuild": "npm run build --prefix front",
    "start": "node back/index.js"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}
